<?php
    // 1.수학 관련 함수 : min, max, floor, round, ceil, rand
    // echo floor(4.9), "\n"; // 소수점 아래 내림
    // echo round(4.5), "\n"; // 소수점 아래 반올림
    // echo ceil(4.1), "\n"; // 소수점 아래 올림
    // echo min( array(3, 4, 6, 1, 3) ), "\n";
    // echo max( array(3, 4, 6, 1, 3) ), "\n";
    // echo rand(0, 2), "\n";

    // 2.날짜 관련 함수
    // echo date("Y-m-d H:i:s")."\n";
    // echo date("y-m-d H:i:s")."\n";
    // echo date("y-m-d h:i:s a")."\n";
    // echo date("y-M-d h:i:s a")."\n";
    // echo date("ymdhis")."\n";

    // 3.난수함수
    // echo mt_rand(0,10000);

    //
    // echo PHP_OS, "\n";
    // echo PHP_VERSION, "\n";
    // var_dump($GLOBALS)."\n";
    // var_dump(phpinfo());
    
    // 상수 선언(명명규칙 : 상수명은 꼭 대문자로 작성한다.)
    define("INT_ONE", 1);

    echo INT_ONE;
    // 4.
?>